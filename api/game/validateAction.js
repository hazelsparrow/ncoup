function validateAction(action, room) {
  // TODO: implement server side validation to make
  // sure that the given action is allowed by the rules of the game at this time
  return true;
}

module.exports = validateAction;
