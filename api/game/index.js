const validateAction = require('./validateAction');
      createDeck = require('./createDeck'),
      handleAction = require('./handleAction');

module.exports = {
  validateAction,
  createDeck,
  handleAction
};
